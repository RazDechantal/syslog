#!/usr/bin/env python3
from logging import root
import sys
import os
import re


def error_search(log_file):
    error = input("What is the error? ")
    returned_errors = []
    return returned_errors



if __name__ == "__main__":
  cvs_file = sys.argv[1]
  path = sys.argv[2]
  returned_errors = error_search(path)
  sys.exit(0)